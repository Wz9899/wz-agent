# wz-agent 环境变量模板。
#
# 使用方法：复制为 .env 并填入真实 Key（.env 已被 .gitignore 忽略，不会上传）：
#   cp .env.example .env
# 或直接在 shell 中设置环境变量（优先级高于 .env）：
#   export LLM_API_KEY="..."          # 通用，优先
#   export DEEPSEEK_API_KEY="sk-..."  # 兼容旧写法（未设 LLM_API_KEY 时回退）

# ---- LLM 配置（OpenAI 兼容协议，默认 DeepSeek）----
# 只需填 API Key，其余可留默认（走 DeepSeek）。

# API Key：优先 LLM_API_KEY，未设则回退 DEEPSEEK_API_KEY
LLM_API_KEY=sk-你的key
# 兼容旧变量（二选一，填了它会自动回退）
DEEPSEEK_API_KEY=sk-你的key

# 服务地址（默认 DeepSeek）
# LLM_BASE_URL=https://api.deepseek.com
# GLM Coding Plan 套餐 key 必须走 coding 端点（普通 /api/paas/v4 会报 429/1113 无可用资源包）：
#   国内：https://open.bigmodel.cn/api/coding/paas/v4
#   全球：https://api.z.ai/api/coding/paas/v4

# 模型名（默认 deepseek-chat）
# LLM_MODEL=deepseek-chat
# 例如 GLM: LLM_MODEL=glm-5.3-flash
